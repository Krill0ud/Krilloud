export const setEditor = (state, editor) => {
  state.editor = editor
}

export const setEngine = (state, engine) => {
  state.engine = engine
}

export const setPosition = (state, position) => {
  state.position = position
}

export const setCurrentPathConnection = (state, io) => {
  state.currentPathConnection = io
}
